import React from 'react';import GradientBackground from '../UI/shapes/GradientBackground';import Topbar from './topbar/Topbar';import Footer from './Footer';import useGlobalMessage from '@/hooks/useGlobalMessage';import GuideStatusSwitcher from './GuideStatusSwitcher';import GuideActiveCallNotification from '@/features/calls/components/GuideActiveCallNotification';import Head from 'next/head';import { pageTitle } from '@/services/utils';const Layout: (props: React.PropsWithChildren<{ hideFooter?: boolean; hideTopbar?: boolean }>) => JSX.Element = ({ children, hideFooter, hideTopbar }) => {  useGlobalMessage();  return (    <>      <Head>        <title>{pageTitle()}</title>      </Head>      <GradientBackground />      {!hideTopbar && <Topbar />}      <main>{children}</main>      {!hideFooter && <Footer />}      <GuideStatusSwitcher />      <GuideActiveCallNotification />    </>  );};export default Layout;