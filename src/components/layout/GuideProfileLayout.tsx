import React from 'react';import Button from '../UI/button/Button';import Illustrations from '@/images/illustrations';import { route } from '@/services/routes';import { useRouter } from 'next/router';import Head from 'next/head';import { pageTitle } from '@/services/utils';const GuideProfileLayout: (props: React.PropsWithChildren) => JSX.Element = ({ children }) => {  const router = useRouter();  return (    <>      <Head>        <title>{pageTitle()}</title>      </Head>      <div className="container relative mb-9 flex items-center justify-center gap-7.5 pt-8 sm:pt-16 [&>a]:sm:w-48">        <Illustrations.IllustrationLogo className="absolute -top-1/4 left-1/2 -z-1 w-[574px] max-w-[80%] -translate-x-1/2" />        <Button href={route.guideProfile()} size="lg" color={router.pathname === route.guideProfile() ? 'gradient' : undefined}>          Profil        </Button>        <Button href={route.guideCalendar()} size="lg" color={router.pathname === route.guideCalendar() ? 'gradient' : undefined}>          Přehled rezervací        </Button>      </div>      {children}    </>  );};export default GuideProfileLayout;