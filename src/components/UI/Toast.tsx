import { CheckCircle, Info, WarningCircle, X } from '@phosphor-icons/react';import { ExternalToast, toast as toastSonner } from 'sonner';export const toast = {  success: (message: string, data?: ExternalToast) => {    toastSonner.custom(      (t) => (        <div style={{ width: 'var(--width)' }} className="flex flex-shrink-0 items-center gap-2 rounded bg-success-900 p-4 text-sm text-white">          <CheckCircle size={24} weight="fill" className="text-success" />          {message}          <button type="button" onClick={() => toast.dismiss(t)} className="ml-auto rounded-full p-1.5 hover:bg-white/10">            <X size={12} className="text-white" />          </button>        </div>      ),      { dismissible: true, onDismiss: (t) => toastSonner.dismiss(t.id), ...data },    );  },  error: (message: string, data?: ExternalToast) => {    toastSonner.custom(      (t) => (        <div style={{ width: 'var(--width)' }} className="flex flex-shrink-0 items-center gap-2 rounded bg-primary-700 p-4 text-sm font-400 text-white">          <WarningCircle size={24} weight="fill" />          {message}          <button type="button" onClick={() => toast.dismiss(t)} className="ml-auto rounded-full p-1.5 hover:bg-white/10">            <X size={12} className="text-white" />          </button>        </div>      ),      data,    );  },  info: (message: string, data?: ExternalToast) => {    toastSonner.custom(      (t) => (        <div style={{ width: 'var(--width)' }} className="flex flex-shrink-0 items-center gap-2 rounded p-4 text-sm font-400 text-white bg-gradient-primary">          <Info size={24} weight="fill" />          {message}          <button type="button" onClick={() => toast.dismiss(t)} className="ml-auto rounded-full p-1.5 hover:bg-white/10">            <X size={12} className="text-white" />          </button>        </div>      ),      data,    );  },  dismiss: (id?: string | number | undefined) => toastSonner.dismiss(id),};