import React, { useState } from 'react';import { IconPlayButton } from '../UI/Icons';import { cn } from '@/services/utils';const HowItWorksVideo = (props: React.HTMLAttributes<HTMLDivElement>) => {  const [isPlaying, setIsPlaying] = useState(false);  const videoId = '4fAdSJbEcUI';  const handlePlayVideo = () => {    setIsPlaying(true);  };  return (    <div {...props} className={cn('relative flex aspect-video justify-center overflow-hidden rounded bg-gradient sm:rounded-xl', props.className)}>      {!isPlaying ? (        <>          {}          <img            src={`https://img.youtube.com/vi/${videoId}/maxresdefault.jpg`}            alt="Jak to funguje - náhled videa"            className="h-full w-full object-cover"          />          {}          <button             onClick={handlePlayVideo}            className="absolute inset-0 flex items-center justify-center bg-black/20 transition-colors hover:bg-black/30"          >            <IconPlayButton className="w-24 transition-transform hover:scale-105 sm:w-32" />          </button>        </>      ) : (        <iframe          src={`https://www.youtube.com/embed/${videoId}?autoplay=1&rel=0&modestbranding=1`}          title="Jak to funguje"          className="h-full w-full"          frameBorder="0"          allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"          allowFullScreen        />      )}    </div>  );};export default HowItWorksVideo;