import Image from 'next/image';import { Phone } from '@phosphor-icons/react';import useGuides from '@/hooks/data/useGuides';import { getImageUrl, guideName } from '@/services/utils';import { GradientTitle } from '../UI/typography/Typography';const GuidesSection: ( ) => JSX.Element = () => {    const { data: guides  } = useGuides();  return (    <section className=" text-white py-20">      <div className="max-w-7xl mx-auto grid lg:grid-cols-2 gap-12 items-start">        {}        <div>          {}           <GradientTitle className="mb-3.5">Komu zavolat</GradientTitle>          <h2 className="text-4xl font-bold mb-4">Naši kvalifikovaní průvodci</h2>          <p className="text-white/80 mb-6 leading-relaxed">            Zkušení průvodci jsou připraveni odpovědět na vaše nejhlubší otázky. Neváhejte se            zeptat na cokoliv – žádná otázka není tabu. Připojte se odkudkoliv a kdykoliv.          </p>          <button className="bg-gradient-to-r from-[#DF7E60] to-[#B526D6] text-white font-semibold px-6 py-3 rounded-full">            Najít svého průvodce          </button>        </div>        {}        <div className="grid grid-cols-3 gap-4">          {guides?.map((guide, index) => (            <div key={index} className="relative rounded-xl overflow-hidden">            {guide.avatar && <Image src={getImageUrl(guide.avatar)} fill className="object-cover object-center" alt={guideName(guide)} />}              {}              {guide.call_status && (                <span className="absolute top-2 right-2 bg-lime-400 text-black text-xs font-semibold px-2 py-0.5 rounded-full">                  guide.call_status                </span>              )}              {}              { (                <span className="absolute bottom-2 left-1/2 -translate-x-1/2 bg-[#B526D6] p-2 rounded-full">                  <Phone size={20} weight="fill" className="text-white" />                </span>              )}            </div>          ))}        </div>      </div>    </section>  );};export default GuidesSection;