import { Calendar, UserCircle } from '@phosphor-icons/react';import Link from 'next/link';interface ReviewDetailProps {  title: string;  content: string;  date: string;  author: string;}export default function ReviewDetail({ title, content, date, author }: ReviewDetailProps) {  return (    <div className="max-w-3xl mx-auto px-4 py-10">      <Link href="/reviews" className="text-sm text-purple-400 hover:underline">← Zpět na recenze</Link>      <div className="mt-4 mb-2 flex items-center gap-2 text-sm text-white/70">        <Calendar size={18} />        <span>{date}</span>        <span className="mx-2">|</span>        <UserCircle size={18} />        <span>{author}</span>      </div>      <h1 className="text-3xl font-bold text-white mb-4">{title}</h1>      <article className="prose prose-invert text-white/90">        <p>{content}</p>      </article>    </div>  );}